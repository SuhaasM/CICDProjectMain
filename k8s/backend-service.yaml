apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: lms
spec:
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 12345
      targetPort: 12345
  type: ClusterIP